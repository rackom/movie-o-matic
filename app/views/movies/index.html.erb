<div class="container">
    <br>
    <div class="header">Movies</div>
</div>

<table class="container">
  <tr>
    <td>
      <%= form_tag movies_path, :method => :get do |f| %>
        <%= text_field_tag :q, @query, 'data-autocomplete-url' => autocomplete_movies_path %>
        <%= submit_tag 'Search' %>
      <% end %>
    </td>
  </tr>
  <tr>
    <th>Name</th>
  </tr>

<%  %>
<% @movies.each do |movie| %>
  <tr>
    <td><%= movie.name %></td>
  </tr>
<% end %>
</table>